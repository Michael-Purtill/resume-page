import React from "react";
import ReactDOM from "react-dom";
import MySite from "./MySite";

import "./styles.css";

const rootElement = document.getElementById("root");
ReactDOM.render(<MySite />, rootElement);
